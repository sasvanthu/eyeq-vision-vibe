# Backend Environment Variables (.env file)
# Copy this file to .env and fill in the actual values
# NEVER commit the actual .env file to version control

# Node Environment
NODE_ENV=production
PORT=5000

# CORS Configuration
CORS_ORIGIN=https://eyeq-simats.vercel.app

# Firebase Admin SDK (Service Account)
# Generate from Firebase Console → Project Settings → Service Accounts
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"eyeq-web","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"firebase-adminsdk-...@eyeq-web.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# Supabase Configuration
# Get from Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Optional: Alternative Firebase Service Account Path
# If using file instead of JSON string
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Notes for Production Deployment (Render):
# 1. Set NODE_ENV=production
# 2. Ensure PORT is exposed and accessible
# 3. FIREBASE_SERVICE_ACCOUNT_JSON must be properly escaped JSON string
# 4. SUPABASE_SERVICE_KEY should ONLY be on backend (never exposed to frontend)
# 5. CORS_ORIGIN should be your Vercel frontend URL
# 6. All credentials must be added as Environment Variables in Render dashboard
